package com.SwagDev.SwagHub.modules.announcements;

/** {@code rotation: sequential|random} (§5.6) for one {@code announcements.yml} world entry. */
public enum Rotation {
    SEQUENTIAL,
    RANDOM
}
